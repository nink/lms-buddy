# LMS Buddy — local connection template
# Copy values into the Connection tab in the UI.
# Secrets are stored in data/config.json (gitignored), never commit that file.

LMS_HOST=192.168.72.70
LMS_PORT=1234
# LMS_API_KEY=

SSH_USER=nink
# SSH_PASSWORD=
# SSH_PRIVATE_KEY_PATH=C:\Users\peter\.ssh\id_ed25519
# SSH_PORT=22
